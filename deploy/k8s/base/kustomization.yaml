apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - server.yaml
  - web.yaml
  - admin.yaml
  - redis.yaml
  - postgres.yaml
  - backup-cronjob.yaml
  - ingress.yaml
# migrate-job.yaml is applied separately by CI BEFORE the rollout.
