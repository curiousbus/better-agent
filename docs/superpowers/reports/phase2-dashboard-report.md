# Phase 2: Usage Analytics Dashboard — Implementation Report

**Status: DONE**

**Date:** 2026-07-01

---

## Summary

Built the usage analytics Dashboard at `/` (root), moved the chat UI to `/chat`, updated the sidebar, and wired up a recharts dual-line token chart against the `orpc.usage.summary` backend procedure.

---

## Files Created

| File | Purpose |
|------|---------|
| `/Users/john/better-agent/apps/web/src/routes/chat.tsx` | Chat UI moved here (identical to old index.tsx, route changed to `/chat`) |
| `/Users/john/better-agent/apps/web/src/components/dashboard/dashboard-constants.ts` | Shared constants: WINDOW_OPTIONS, WindowDays, DEFAULT_WINDOW, CENTS_PER_DOLLAR, chart colors |
| `/Users/john/better-agent/apps/web/src/components/dashboard/use-usage-data.ts` | Hook: queries `orpc.usage.summary`, builds continuous day axis, merges sparse rows |
| `/Users/john/better-agent/apps/web/src/components/dashboard/summary-card.tsx` | Single stat card with skeleton loading |
| `/Users/john/better-agent/apps/web/src/components/dashboard/summary-cards.tsx` | 3-column grid: Total Tokens, Est. Cost, Turns |
| `/Users/john/better-agent/apps/web/src/components/dashboard/token-chart.tsx` | Recharts dual-line chart (Input/Output tokens) with skeleton loading |
| `/Users/john/better-agent/apps/web/src/components/dashboard/empty-state.tsx` | Zero-usage placeholder |
| `/Users/john/better-agent/apps/web/src/components/dashboard/window-toggle.tsx` | 3D / 7D / 12D pill toggle |

## Files Modified

| File | Change |
|------|--------|
| `/Users/john/better-agent/apps/web/src/routes/index.tsx` | Replaced chat UI with Dashboard route |
| `/Users/john/better-agent/apps/web/src/components/sidebar.tsx` | Home→Dashboard (Gauge), added Chat (MessageSquare at /chat) |
| `/Users/john/better-agent/apps/web/src/routeTree.gen.ts` | Auto-regenerated by TanStack Router watcher; /chat route added |
| `/Users/john/better-agent/apps/web/package.json` | Added `"recharts": "2.15.3"` (exact pin) |
| `/Users/john/better-agent/pnpm-lock.yaml` | Updated by pnpm install |

---

## Chart Path: recharts 2.15.3

Took the **recharts path**. Version `2.15.3` installed (exact pin, no `^` or `~`). No peer dependency conflicts with React 19. Build and type-check both passed cleanly.

Chart renders two `<Line>` components (Input tokens in indigo `#6366f1`, Output tokens in amber `#f59e0b`) inside a `<ResponsiveContainer>` with `<CartesianGrid>`, `<XAxis>`, `<YAxis>`, `<Tooltip>`, and `<Legend>`.

---

## Check Command Results

### ultracite fix
```
Checked 10 files in 89ms. No fixes applied.
```
**PASS**

### pnpm -F web check-types
```
$ tsc --noEmit
(no output = 0 errors)
```
**PASS — 0 errors**

### npx eslint (new/changed files)
```
ESLint: 0 errors, 1 warnings in 1 files

Top rules:
  max-params (1x)

Top files:
  src/routes/chat.tsx (1 issues)
    max-params (1)
```
**PASS — 0 errors**

The 1 warning (`max-params` on `useHomeActions` in chat.tsx) is pre-existing in the original `routes/index.tsx` and carried over verbatim as required by the spec ("copy the file, change only the route path"). Confirmed pre-existing by stashing and linting the original.

### pnpm -F web test
```
 RUN  v4.1.9

 Test Files  6 passed (6)
      Tests  28 passed (28)
   Duration  498ms
```
**PASS — all 28 tests passing**

---

## Line Limit Notes

**token-chart.tsx** — `TokenChart` was initially 54 lines (over the 50-line limit), flagged by ESLint as an error. Fixed by extracting `ChartBody` component for the `ResponsiveContainer`+`LineChart` subtree. Both `TokenChart` and `ChartBody` are now under 50 lines.

**summary-cards.tsx** — `no-magic-numbers` warnings for `1_000_000`, `1_000`, and decimal places. Fixed by extracting `MILLION`, `THOUSAND`, `COST_DECIMAL_PLACES`, and `TOKEN_DECIMAL_PLACES` constants.

**use-usage-data.ts** — `no-magic-numbers` for `.slice(0, 10)`. Fixed by extracting `ISO_DATE_LENGTH = 10`.

**dashboard-constants.ts** — The `WINDOW_OPTIONS = [3, 7, 12] as const` literal was flagged. Suppressed with `// eslint-disable-next-line no-magic-numbers` (the constant definition is the only appropriate place for these values).

All files are well under 300 lines.

---

## Route Tree

TanStack Router's file watcher auto-regenerated `routeTree.gen.ts` when `routes/chat.tsx` was created — no manual editing was required. The `/chat` route is fully registered in all type maps (`FileRoutesByFullPath`, `FileRoutesByTo`, `FileRoutesById`, `FileRouteTypes`, `RootRouteChildren`).

---

## Anything Unfinished / Uncertain

- The `orpc.usage.summary` procedure is consumed via `orpc.usage.summary.queryOptions({ input: { windowDays } })` — this matches the spec exactly. If the backend type differs at runtime from what's typed, the chart will show 0s (the hook defaults to empty totals on missing data).
- No browser testing performed (per project memory: "finish UI work + notify user to test; don't drive chrome-devtools").
- The existing tests (6 files, 28 tests) all pass and are unchanged.
